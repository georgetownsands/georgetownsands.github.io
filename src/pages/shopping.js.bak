import * as React from "react"
import { graphql, useStaticQuery } from "gatsby"
import Layout from "../components/layout"
import Seo from "../components/seo"

const Shopping = () => {
  // const areaData = useStaticQuery(graphql`
  //   query SiteQuery {
  //     site {
  //       siteMetadata {
  //         title
  //         menuLinks {
  //           name
  //           link
  //         }
  //         area {
  //           section
  //           data {
  //             id
  //             name
  //             detail {
  //               id
  //               name
  //               phoneNumber
  //               address
  //               description
  //               webSite
  //               searchWords
  //             }
  //           }
  //         }
  //       }
  //     }
  //   }
  // `)
  return (
    <Layout>
      <Seo title="Shopping" />
      <h3>Shopping</h3>
      <ul style={{ display: "flex", flex: 1 }}>
        {/* {areaData.site.siteMetadata.area
          .filter(a.section === 6)
          .map(filteredSection =>
            filteredSection.data.map(s => <li>{s.name}</li>)
          )} */}
      </ul>
    </Layout>
  )
}

export default Shopping
